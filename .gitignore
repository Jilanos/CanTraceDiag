.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
dist/
build/
*.egg-info/

# Local acquisition data can be large and may contain vehicle/customer data.
data/
traces/
*.asc
*.blf
*.mf4
*.dbc
*.cfg
*.cfg.ini
*.stcfg
*.parquet
*.duckdb

# IDE/editor
.idea/
.vscode/
*.swp
AGENTS.md
LOGICS.md

# Locally bootstrapped Python deps (Python packaging is broken system-wide)
.pydeps/

# Synthetic, anonymized test fixtures are safe to commit (override *.asc/*.dbc above).
!tests/fixtures/*.asc
!tests/fixtures/*.dbc

# Playwright browser-lib workaround (local, no sudo) and any browser artifacts
.pw-libs/

# Generated by scripts/install-shortcut.ps1 for the local Windows machine.
CanTraceDiag.local.cmd

# Generated by spikes/pwa-local-engine/build-browser.mjs from tracked PWA
# sources and the product web shell. Keep the source files, not the built site.
spikes/pwa-local-engine/browser/
spikes/pwa-local-engine/site/
